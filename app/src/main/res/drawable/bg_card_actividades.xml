<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Capa de sombra -->
    <item android:right="2dp" android:left="2dp" android:top="2dp" android:bottom="2dp">
        <shape>
            <solid android:color="#000000" />
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Capa del contenedor principal -->
    <item>
        <shape>
            <solid android:color="#2A2A2A" /> <!-- Fondo más suave que #1B1B1B -->
            <stroke
                android:width="2dp"
                android:color="#FFD700" /> <!-- Dorado fuerte -->
            <corners android:radius="12dp" />
        </shape>
    </item>
</layer-list>
